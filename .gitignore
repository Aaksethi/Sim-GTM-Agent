# Dependencies — Render/Netlify reinstall these, never commit them
node_modules/

# Secrets — your API keys live in the Render/Netlify dashboards, never in git
.env
.env.local
.env.production
clay-oauth.json

# Build output
dist/

# Logs
*.log
